{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        //TODO: removed this vvv to get back to initial state
        //"emitDecoratorMetadata": true,
        "outDir": "../../out-tsc/spec",
        "types": ["jest", "node"]
    },
    "files": ["src/polyfills.ts"],
    "include": ["src/**/*.spec.ts", "src/**/*-spec.ts", "src/**/*.d.ts"]
}
